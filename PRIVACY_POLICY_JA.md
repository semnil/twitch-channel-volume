# プライバシーポリシー — Twitch Channel Volume

[English version here](PRIVACY_POLICY.md)

最終更新日: 2026-08-22

## 概要

Twitch Channel Volume は、Twitch のチャンネルごとに音量（ゲイン）を記憶・自動適用する Chrome 拡張機能です。再生中の音声を ITU-R BS.1770 規格でラウドネス計測し、ターゲット LUFS に基づいて音量を自動調整します。本ポリシーでは、拡張機能が扱うデータの種類、利用目的、保存先について説明します。

## 収集するデータと目的

### チャンネル音量設定

- **対象**: Twitch チャンネル識別子（ページから取得できた後は数値の owner ID / broadcaster ID、取得できるまでは login 名・動画 ID）、表示名、ページ URL、種別（Live / VOD）ごとに設定したゲイン値、そのチャンネルの種別ごとの LUFS 自動追従のオン・オフ、および直近のラウドネス計測値と、その計測時刻・値が立っている計測窓の数・プレイヤー音量を最大にしたときの値へ換算してあることの記録のキャッシュ。あわせて、仮の識別子から数値の識別子への対応と、フィールドの更新順序を決めるカウンタを保存します
- **目的**: 保存済みチャンネルの配信・動画を開いた時に、設定した音量を自動適用
- **保存先**: デバイス上の `chrome.storage.local` にローカル保存。外部サーバーには一切送信されません

### 拡張機能の設定

- **対象**: ターゲット LUFS 値、CM ゲイン (dB)、表示単位（% または dB）、ゲイン表示のオン・オフ、種別（Live / VOD）ごとの LUFS 自動追従の既定値
- **目的**: ユーザーの好みに応じた動作のカスタマイズ
- **保存先**: デバイス上の `chrome.storage.local` にローカル保存

### 再生音声のラウドネス計測

- **対象**: 再生中の `<video>` 要素の音声から Web Audio API で算出する Momentary / Short-term / Integrated LUFS 値
- **目的**: ラウドネス情報の表示および推奨ゲインの算出
- **保存**: 音声波形やオーディオデータは保存しません。Momentary / Short-term 値はページを開いている間のみメモリに保持します。Integrated 値は操作の有無に関わらず保存します。チャンネルを識別できている間、視聴中の種別の直近値を最短 5 秒間隔でチャンネル設定へ書き込み、あわせて計測時刻と、その値が計測窓いくつぶんに立っているか、およびプレイヤー音量を最大にしたときの値へ換算してあることの記録も書き込みます（つまみをどの位置にしていても換算します）。その種別の LUFS 自動追従が ON の間は、算出したゲインと、そのゲインについての同じ記録も書き込みます。「チャンネルに適用」が保存するのはゲインで、計測値の保存はこの操作によりません

### Twitch ページ情報（読み取りのみ）

- **対象**: Twitch 自身が発行する GraphQL レスポンス（チャンネルの owner ID / broadcaster ID・login 名・表示名の取得用）、プレイヤーが自身のページへ投げる CM の cue（CM 区間の開始と終了の取得用）、ページ内のプレイヤーの CM 表示の有無
- **目的**: チャンネルの永続的な識別子の取得、ポップアップと設定画面でのチャンネル名の表示、および CM 区間の検出
- **保存**: GraphQL レスポンスと cue は保存しません。必要な値（チャンネルの数値 ID・login 名・表示名、リクエスト対象のコンテンツ識別子（ライブは login 名、VOD は動画 ID）、および CM 区間の開始・終了・種別（プリロール / ミッドロール）・pod 内の位置と本数）のみを抽出して利用します。このうち数値 ID はチャンネル設定のキーであり、表示名・login 名・login 名から作るチャンネル URL はその値として保存します。コンテンツ識別子も保存します。数値 ID を取得するまではこのキーの代わりになり、取得後も数値 ID への対応表に残ります。メモリ内だけで扱うのは CM 区間そのもの、すなわち開始・終了・種別・pod 内の位置で、これらはページを開いている間の利用に留まり保存しません。cue に含まれる広告の識別子・広告主名・トラッキング URL は読み出しません

## 収集しないデータ

- 閲覧履歴、アナリティクス、テレメトリは**収集しません**
- Twitch やその他のサイトでの閲覧ページは**追跡しません**
- 音声・映像データそのものを**記録・送信しません**
- 拡張機能の開発者はユーザーのデータを**受信・保存・アクセスしません**
- データの販売、第三者との共有、広告目的での利用は**一切行いません**

## データの送信先

ありません。この拡張機能は**外部へのネットワークリクエストを一切行いません**。GraphQL レスポンスは Twitch のページ自身が発行する通信を読み取るだけであり、拡張機能が新たな外部送信を発生させることはありません。すべてのデータはデバイス上に留まります。

## データの保存とセキュリティ

- すべての設定は `chrome.storage.local` に保存され、この拡張機能のみがアクセス可能です
- デバイス間での同期やクラウドへの保存は行いません
- 拡張機能をアンインストールすると、ローカルに保存されたすべてのデータが削除されます

## 権限

| 権限 | 理由 |
|---|---|
| **storage** | チャンネル音量設定とユーザー設定をローカルに保存 |
| **host_permissions** (`twitch.tv`) | Twitch ページにコンテンツスクリプトを注入し、再生音声の計測と Web Audio API による音量制御を実行 |

## リモートコード

この拡張機能は**リモートコードを使用しません**。すべての JavaScript は拡張機能パッケージ内にローカルで同梱されています。`page-bridge.js` はページのメインワールド (`"world": "MAIN"`) で実行されますが、これは AudioContext の制御と Twitch のページデータを読み取るためのローカルコードであり、リモートから取得されたものではありません。

プレイヤーの CM の cue を受け取るため、`page-bridge.js` はページの `Worker` コンストラクタを包み、ページが作る worker に message リスナーを足します。worker 自体はページが渡した引数のまま生成します。worker のスクリプトを読むことも、書き換えることも、差し替えることもありません。サーバーから取得するコードもありません。

## 単一用途

この拡張機能の用途は1つです: ITU-R BS.1770 ラウドネス計測と Web Audio API を使用して、**Twitch のチャンネルごとの音量設定を計測・記憶・自動適用する**こと。

## 第三者依存

なし。外部ライブラリ、SDK、CDN、アナリティクスツールは一切使用していません。

## ポリシーの変更

変更がある場合は更新日を改訂の上、本ページに掲載します。変更後も拡張機能を継続使用した場合、改訂後のポリシーに同意したものとみなします。

## お問い合わせ

本プライバシーポリシーに関するご質問は、GitHub リポジトリの [Issue](https://github.com/semnil/twitch-channel-volume/issues) にてお寄せください。
